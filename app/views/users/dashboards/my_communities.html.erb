<nav class="navbar navbar-expand-lg navbar-light bg-light">
<h1>My Communities</h1> &nbsp;&nbsp;
  
  <%= link_to 'Create Community', new_users_community_path, class: 'btn btn-primary' %>&nbsp;&nbsp;

  <%= link_to 'Home', users_dashboards_index_path, class: 'btn btn-primary' %>&nbsp;&nbsp;
 
  <%= link_to 'Profile', edit_user_registration_path(current_user), class: 'btn btn-primary' %>&nbsp;&nbsp;

  <%= link_to 'Sign Out', destroy_user_session_path, method: "delete", class: 'btn btn-primary' %>&nbsp;&nbsp;

</nav>

<br></br>
<table class="table">
	<thead class="thead-dark">
    <tr>
      <th scope="col">Community Name</th>
      <th scope="col">Created On</th>
      <th scope="col">Invite Users</th>
      <th scope="col">Open</th>
    </tr>
  </thead>
  
  <tbody>
  <% @community.each do |com| %>
    <tr>
      <td><%= com.name %></td>
      <td><%= com.created_at %></td>
      <td><%= link_to 'Invite', invite_user_users_community_path(com), :remote => true, class: 'btn btn-primary'%></td>
      <td><%= link_to 'Show', users_community_path(com) %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<div id="popup-container">
</div>
